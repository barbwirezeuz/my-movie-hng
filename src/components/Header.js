// Header.js
import React from 'react';

function Header() {
  return (
    <header className="header">
      {/* Header content with background photo */}
    </header>
  );
}

export default Header;
